- const titleS = require("/src/components/01-elements/title/styles/index.json");

mixin title(props = {text: 'Title', isAccent: false})
  h1(class=`${titleS.title} ${props.isAccent ? titleS.title_accent : ''}`)= props.text
